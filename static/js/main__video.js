let video = document.querySelectorAll('.main__video');

let count = 0;

let leftArrow = document.querySelector('.main__arrow.left');
    leftArrow.addEventListener('click', videoSlider);

let rightArrow = document.querySelector('.main__arrow.right');
    rightArrow.addEventListener('click', videoSlider);

    function videoSlider(e){
        if(e.target == rightArrow){
            
        }
    }



